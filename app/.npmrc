# Tiptap 2.9.x declares peerDependencies on react/react-dom ^17 || ^18; this
# app is on React 19. Without this flag, `npm install` fails with ERESOLVE.
# Safe to remove once Tiptap ships a React 19-compatible peer range.
legacy-peer-deps=true
